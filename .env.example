# Django
SECRET_KEY=your-secret-key-change-in-production-at-least-50-chars-long
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_NAME=pos_db
DB_USER=pos_user
DB_PASSWORD=strongpassword123
DB_HOST=db
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80

# Optional: Media files path
# MEDIA_ROOT=/app/media
